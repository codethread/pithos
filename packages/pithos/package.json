{
	"name": "@pithos/pithos",
	"version": "0.1.0",
	"private": true,
	"bin": {
		"pithos": "./bin/pithos"
	},
	"type": "module",
	"exports": {
		".": "./src/index.ts",
		"./builtins": "./src/builtins.ts"
	},
	"scripts": {
		"clean": "rimraf dist bin/pithos bin/pithos-dev *.tsbuildinfo",
		"lint": "eslint .",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run --config vitest.config.ts",
		"build": "node scripts/build.mjs",
		"start": "node scripts/build.mjs --dev --run --"
	},
	"dependencies": {
		"@effect/cli": "catalog:",
		"@effect/platform-node": "catalog:",
		"better-sqlite3": "^12.9.0",
		"effect": "catalog:"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.0"
	}
}
