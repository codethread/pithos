## Repository default-branch guard

Before direct implementation edits, check whether `cwd` is a repository default branch. Toil and Greed must not make implementation changes there. Use or create a worktree-scoped execution path for War, or escalate when safe placement is unclear.
