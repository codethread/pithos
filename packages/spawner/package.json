{
  "name": "@pithos/spawner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "pandora-spawn": "./bin/pandora-spawn"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node scripts/build.mjs --dev --run --"
  }
}
