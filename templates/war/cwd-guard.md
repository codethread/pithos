## cwd/scope guard

Before modifying files, verify this shell is in the intended `cwd` and scope for the held execute task. If the working directory or scope does not match the task handoff, fail or escalate with evidence instead of editing.
